# train and val datasets (image directory or *.txt file with image paths)
train: fmld/train.txt
val: fmld/valid.txt
test: fmld/test.txt

# number of classes
nc: 3

# class names
names: ['masked_face', 'unmasked_face', 'incorrectly_masked_face']
